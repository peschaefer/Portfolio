import React from "react";
import Header from "../components/Header";

const Resume = () => {
    return (
        <div className="App">
            <Header/>
        </div>
    );
};

export default Resume;